import * as React from 'react'

export default class AvatarLayout extends React.Component {
  render () {
    return (
      <div>
        AvatarLayout
        {this.props.children}
      </div>
    )
  }
}
