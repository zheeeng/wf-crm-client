import * as React from 'react'

export default class AllContactLayout extends React.Component {
  render () {
    return (
      <div>
        AllContactLayout
        {this.props.children}
      </div>
    )
  }
}
